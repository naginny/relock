{% extends "admin/basic.html.twig" %}

{% block content %}
    <div class="row">
        <h2>New admin for Re-Lock!</h2>
    </div>
    <br>
    {% if shouldChange == true %}
    <div class="row">
        <div class="col-lg-12">
        <div class="alert alert-danger alert-dismissible fade show">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">×</span></button>
            Your password is not secure. Please change the password <a href="{{ path('fos_user_change_password') }}">here</a>.
        </div>
        </div>
    </div>
    {% endif %}
{% endblock %}